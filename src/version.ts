/**
 * Application Version Management
 * Single source of truth for FlightMap versioning.
 * Bumps on every release/commit starting from v1.0.1.
 */
export const APP_VERSION = '1.0.8';
export const APP_RELEASE_STRING = `RELEASE v${APP_VERSION} STABLE • 64-BIT WEBGL ENGINE`;
